import { View, Text } from 'react-native';
import React from 'react';
import { patternHistoryArray } from './patternHistoryArrayTEST.js';

//TODO - for each pattern in patternHistoryArray, find the one with the highest total missed shots, and return the name of that pattern

const reccomendPattern = () => {
  patternHistoryArray.forEach((pattern) => {});
};

export const recommendPatternAlgorithm = () => {
  return (
    <View>
      <Text>recommendPatternAlgorithm</Text>
    </View>
  );
};
